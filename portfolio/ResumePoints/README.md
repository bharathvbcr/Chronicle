# ResumePoints — retired mirror

This path is **retired**. Resume point banks now live in the Chronicle vault:

```
10-Work/ResumePoints/
```

(legacy `kb/notes/ResumePoints/` still dual-read until cutover; e.g. under `demo-vault/` or your Syncthing `CHRONICLE_DIR`).

Do not sync from `KnowledgeBase/Docs/ResumePoints/` into this folder anymore. Edit notes under `10-Work/ResumePoints/` in the vault.
